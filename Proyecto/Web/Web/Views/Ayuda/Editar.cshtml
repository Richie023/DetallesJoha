@model Web.Entidades.Ayuda

@{
    ViewBag.Title = "Editar";
    Layout = "~/Views/Shared/_LayoutExterno.cshtml";
}

<div class="container mt-5">
    <h2 class="text-center font-weight-bold">Editar Ayuda</h2>

    @using (Html.BeginForm("Editar", "Ayuda", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.HiddenFor(model => model.id)
        <div class="form-group">
            <label for="categoria">Categoría</label>
            @Html.TextBoxFor(model => model.categoria, new { @class = "form-control", @placeholder = "Categoría" })
        </div>
        <div class="form-group">
            <label for="titulo">Título</label>
            @Html.TextBoxFor(model => model.titulo, new { @class = "form-control", @placeholder = "Título" })
        </div>
        <div class="form-group">
            <label for="contenido">Contenido</label>
            @Html.TextAreaFor(model => model.contenido, new { @class = "form-control", @placeholder = "Contenido" })
        </div>
        <div class="form-group">
            @Html.Label("Imagen")
            <div class="row">
                <div class="col-8">
                    <input type="file" id="ImagenAyuda" name="ImagenAyuda" class="form-control form-control-user">
                </div>
                <div class="col-2 text-center" style="margin-right:10%;">
                    @if (!string.IsNullOrEmpty(ViewBag.imagen_url))
                    {
                        <img src="@ViewBag.imagen_url" alt="Imagen actual" class="img-thumbnail" style="width:80px; height:80px;">
                    }
                    else
                    {
                        <span>No hay imagen disponible</span>
                    }
                </div>
            </div>
        </div>
        <div class="text-right">
            <button type="submit" class="primary-btn">Actualizar</button>
            <a href="@Url.Action("Ayuda", "ayuda")" class="primary-btn">Cancelar</a>
        </div>
    }
</div>

