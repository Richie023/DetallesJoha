@model Web.Entidades.Ayuda

@{
    ViewBag.Title = "Registrar Ayuda";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="ayuda-container">
    <div class="ayuda-header">
        <h3>Registrar Ayuda</h3>
        <h4>Especifica los detalles de la ayuda. El contenido se convertirá en una lista de viñetas automáticamente.</h4>
    </div>

    @using (Html.BeginForm("Insertar", "Ayuda", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div class="form-group mb-4">
            <label for="categoria" class="font-weight-bold">Categoría</label>
            @Html.TextBoxFor(model => model.categoria, new { @class = "form-control", @placeholder = "Ej. Plataforma, Registro, Perfil" })
        </div>

        <div class="form-group mb-4">
            <label for="titulo" class="font-weight-bold">Título</label>
            @Html.TextBoxFor(model => model.titulo, new { @class = "form-control", @placeholder = "Título de la ayuda" })
        </div>

        <div class="form-group mb-4">
            <label for="contenido" class="font-weight-bold">Contenido</label>
            @Html.TextAreaFor(model => model.contenido, new
            {
                @class = "form-control",
                rows = "8",
                @placeholder = "Escribe cada punto en una línea. Se mostrará como lista con viñetas."
            })
        </div>

        <div class="form-group mb-4">
            <label for="ImagenAyuda" class="font-weight-bold">Imagen (opcional)</label>
            <input type="file" id="ImagenAyuda" name="ImagenAyuda" class="form-control" accept="image/*" onchange="previewImagen(this)">
            <div class="mt-3 text-center">
                <img id="previewImagen" src="#" alt="Vista previa" style="max-width: 300px; display: none;" class="img-fluid ayuda-imagen" />
            </div>
        </div>

        <div class="text-right">
            <button type="submit" class="primary-btn bg-success text-white">Guardar</button>
            <a href="@Url.Action("Ayuda", "Ayuda")" class="primary-btn bg-secondary text-white">Cancelar</a>
        </div>
    }

    @if (ViewBag.MsjPantalla != null)
    {
        <div class="alert alert-danger mt-4 text-center">
            @ViewBag.MsjPantalla
        </div>
    }
</div>

<script>
    function previewImagen(input) {
        const preview = document.getElementById('previewImagen');
        if (input.files && input.files[0]) {
            const reader = new FileReader();
            reader.onload = function (e) {
                preview.src = e.target.result;
                preview.style.display = 'block';
            };
            reader.readAsDataURL(input.files[0]);
        }
    }
</script>


